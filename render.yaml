services:
  - type: web
    name: universa-timeline-api
    runtime: python
    rootDir: backend/timeline_engine
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
