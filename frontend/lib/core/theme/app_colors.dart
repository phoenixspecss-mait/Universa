import 'package:flutter/material.dart';

/// Centralized color constants for the UNIVERSA forensic platform.
abstract class AppColors {
  // Backgrounds
  static const background = Color(0xFF0F1115);
  static const surface = Color(0xFF1B1F27);
  static const surfaceLight = Color(0xFF232836);
  static const canvasBlack = Color(0xFF0A0E16);

  // Borders
  static const border = Color(0x14FFFFFF);
  static const borderLight = Color(0x29FFFFFF);

  // Primary — Navy
  static const primaryNavy = Color(0xFF1E3A8A);
  static const primaryNavyLight = Color(0xFF2563EB);

  // Teal / Green — Verified states
  static const tealGreen = Color(0xFF1F7A5C);
  static const tealGreenLight = Color(0xFF10E7A0);
  static const emerald = Color(0xFF10B981);

  // Amber — AI / Detection / Processing
  static const rustAmber = Color(0xFFB5551F);
  static const rustAmberLight = Color(0xFFF59E0B);

  // Utility
  static const electricCyan = Color(0xFF00D2FF);
  static const error = Color(0xFFEF4444);
  static const white = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFFE2E8F0);
  static const textSecondary = Color(0xFF94A3B8);
  static const textMuted = Color(0xFF64748B);

  // Grid overlay
  static const dotGrid = Color(0x14FFFFFF);
}
